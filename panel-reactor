总体判断
当前 TUI 已具备完整功能，但界面模型偏“功能堆叠”，主要问题不是颜色，而是：
- 导航层级混乱
- 全局与子页面输入处理冲突
- 小终端适配不足
- 数据列表缺乏统一滚动模型
- 配置编辑存在数据安全风险
- app.js 已达到 1104 行，维护成本较高
建议采用“键盘优先、鼠标辅助、统一焦点管理”的方向，不建议继续在现有 app.js 上堆叠功能。
一、当前界面问题
 1. 顶部有 10 个 Tab，宽度紧张时容易溢出；第 10 个 Tab 没有数字快捷键。见 panel/app.js:131-142、737-741。
 2. 侧栏始终显示服务操作，即使当前页面是监控、模块、聊天或数据页，操作仍然无效。见 panel/app.js:395-403。
 3. 子页面和 App 根组件同时注册 useInput，可能出现一次按键触发多个行为。见 app.js:640-994、ChatView.js、DbView.js、ModulesView.js。
 4. 主区按上下键时仍可能改变侧栏焦点，随后 Enter 执行隐藏的侧栏操作。
 5. Setup 未完成时仍可以通过数字键或 Tab 离开向导。
 6. Setup 的多选字段没有真正的二级选择器，无法直观勾选任意模块或服务。见 SetupView.js:308-325。
 7. 多处硬编码 127.0.0.1:3001，自定义 DB 端口时监控、聊天、模块、数据等页面会失效。见 ChatView.js、DbView.js、ModulesView.js、monitor.js。
 8. server.properties 保存时会重写整个文件，未知字段、注释和未纳入 schema 的配置都会丢失。见 app.js:43-79、526-580。
 9. Chat、DB、监控玩家列表只显示首屏，没有统一滚动和分页。
10. 帮助页不是严格模态层，打开后部分按键仍会继续传递给其他逻辑。
11. 右键提示“已复制”，但鼠标处理实际上没有复制内容。见 app.js:292、mouse.js:23-30。
12. theme.js 中 text、muted、primary、secondary、info 基本使用同一颜色，视觉层级不足。
13. 固定侧栏宽度 20、Setup 宽度 60、固定高度计算，在 80x24 或更小终端中可能溢出。
14. DB 直连模式使用 CommonJS require 和未定义的 __dirname，在当前 ESM 项目中基本不可用。见 DbView.js:7-13、37-76。
二、目标界面结构
建议将当前 10 个顶栏 Tab 改成 6 个主区域：
- 总览
- 服务
- 监控
- 模块
- 数据
- 设置
服务 BDS、DB-Server、QQ-Bridge、LLBot 不再各占一个顶栏 Tab，而是在“服务”页中作为列表或子标签展示。
整体布局建议：
┌──────────────────────────────────────────────────────────────┐
│ SFMC Panel   状态摘要                         时间 / 版本   │
├──────────────┬───────────────────────────────────────────────┤
│ 主导航       │ 页面标题                    页面状态          │
│              ├───────────────────────────────────────────────┤
│ 总览         │                                               │
│ 服务         │                  当前页面内容                 │
│ 监控         │                                               │
│ 模块         │                                               │
│ 数据         │                                               │
│ 设置         │                                               │
├──────────────┴───────────────────────────────────────────────┤
│ ↑↓选择  Enter确认  ←→切换区域  /搜索  ?:帮助  q:退出          │
└──────────────────────────────────────────────────────────────┘
底部命令栏只在服务日志页或明确需要命令输入时显示，普通页面不应始终占用 4 行空间。
三、交互模型重构
优先解决输入系统，而不是继续添加快捷键。
建议：
- App 根组件只保留一个全局 useInput。
- 子页面不再直接注册 useInput。
- 子页面返回当前焦点、可执行动作和动作回调。
- 建立统一状态：
- screen
- focusRegion
- focusedIndex
- overlay
- inputMode
- pendingAction
- 输入优先级固定为：
1. 确认框
2. 文本编辑
3. 搜索框
4. 页面局部导航
5. 全局快捷键
- 主区上下键只操作主区。
- 侧栏上下键只操作侧栏。
- Enter 只能执行当前焦点对象。
- 所有危险操作必须经过统一确认框。
- Esc 按层级返回，而不是直接跳 Dashboard。
- Setup 未完成时锁定主导航，只允许退出或完成向导。
建议的快捷键：
- Tab：切换焦点区域
- ↑↓：列表导航
- ←→：切换列或子区域
- Enter：确认或进入
- Esc：返回上一级
- /：搜索
- r：刷新
- ?：帮助
- q：退出
- Ctrl+C：立即退出并清理
四、各页面优化方案
总览页
增加真正的控制台信息：
- 服务状态卡片
- BDS 在线玩家数
- TPS / MSPT
- 系统 CPU / 内存
- 最近错误数量
- 最近日志
- 未完成初始化、服务异常、模块依赖缺失等告警
- 快捷操作：启动全部、停止全部、重启异常服务
不要直接把日志作为 Dashboard 的主要内容。
服务页
统一展示所有服务：
- 服务名称
- 当前状态
- PID
- CPU / 内存
- 最近启动时间
- 最近退出原因
- 自动重启状态
- 依赖状态
操作包括：
- 启动
- 停止
- 重启
- 查看日志
- 清理日志
- 打开配置
服务操作只在选中服务后出现，避免当前页面显示无效按钮。
监控页
改为固定信息卡片加表格：
- 系统资源卡片
- 服务资源表格
- TPS / MSPT 卡片
- 玩家表格
- 实体与区块统计
- 数据更新时间
- 数据来源状态
玩家表格需要：
- 上下滚动
- PageUp/PageDown
- 总人数和当前范围
- 玩家名称、维度、区块、实体
- 监控接口断开时显示“数据已过期”，不能继续显示旧数据而不提示
模块页
建议增加：
- 按类型筛选
- 按启用、禁用、未安装筛选
- 搜索模块
- 依赖状态列
- 选中模块的详细信息区
- 操作前确认
- 操作中状态
- 操作完成后自动刷新
- 依赖链展示
- 被依赖模块提示
当前模块列表只有长文本描述，信息密度较高但扫描效率低。
聊天页
建议增加：
- 频道列表独立滚动
- 消息列表独立滚动
- 消息时间
- 消息来源类型
- 长消息自动换行
- Home/End 定位
- l 切换实时跟随
- 暂停实时刷新
- 当前消息数量
- 频道搜索
当前通过 slice() 截断消息，容易造成重要内容不可见。见 ChatView.js:107-123。
数据页
建议实现统一表格组件：
- 表列表可滚动
- 表数据可分页
- 行号
- 列宽自适应
- 长文本横向滚动或折叠
- r 刷新
- b 返回
- 当前页码
- 当前查询条件
直连模式需要统一改为 ESM 写法，或者暂时移除直连模式，避免提供一个实际上不可用的选项。
配置页
配置编辑需要分成：
- 配置文件列表
- 配置分类
- 字段编辑
- 修改预览
- 保存确认
- 重启提示
每个字段需要支持：
- 类型校验
- 最小值和最大值
- 必填检查
- 枚举说明
- 修改前后差异
- 保存失败提示
server.properties 必须改为增量保存，保留：
- 未知字段
- 原始注释
- 字段顺序
- 空行
- 未纳入 schema 的新版本字段
保存前显示 diff，并自动创建带时间戳的备份。
Setup 向导
建议改为真正的全屏向导：
- 每一步显示完成状态
- 当前步骤只显示相关内容
- DB 连接失败时明确显示原因
- 路径检测显示结果
- 模块选择使用独立复选列表
- 服务选择使用独立复选列表
- 提交前显示完整配置摘要
- 提交前二次确认
- 提交失败时保留当前输入
- 未完成时禁止离开其他页面
五、视觉系统优化
当前主题颜色层级不足，建议定义明确语义：
- bg：终端背景
- surface：页面背景
- surfaceRaised：卡片背景
- text：正文
- muted：辅助文本
- primary：当前焦点
- accent：标题与关键动作
- success：正常
- warning：警告
- error：错误
- info：提示
- border：普通边框
- borderFocus：焦点边框
建议：
- 焦点使用背景色加左侧指示符，不只改变文字颜色。
- 标题、正文、辅助说明使用明显亮度差。
- 错误和警告同时使用颜色、符号和文字，避免纯颜色依赖。
- 尽量减少 emoji，避免不同终端字体导致宽度错位。
- 中文、英文、数字混排时统一使用终端显示宽度计算，而不是 JavaScript 的 String.length。
- 日志级别使用固定宽度列，提升扫描效率。
六、响应式策略
至少支持三种终端模式：
- 宽屏：120 列以上，显示侧栏、详细信息和多列表格。
- 标准：80 至 119 列，隐藏次要说明，保留核心信息。
- 窄屏：小于 80 列，切换为单列页面，侧栏改为顶部选择器。
高度处理必须增加下限保护：
- 小于 24 行时显示终端过小提示。
- 禁止出现负高度。
- 所有列表统一按可用高度计算。
- 页面内容必须支持滚动，不再直接截断。
- 顶栏标签在空间不足时使用滚动 Tab 或下拉式导航。
七、网络与状态管理
增加统一 API 客户端，例如：
panel/api/client.js
统一负责：
- DB host 和 port
- 请求超时
- JSON 解析
- 错误格式化
- 重试
- 请求取消
- 当前连接状态
DB 地址必须来自单一配置源，优先级建议：
1. 显式环境变量
2. panel-state.json
3. db_config.json
4. 默认值 127.0.0.1:3001
所有页面不得再单独定义 DB_PORT = 3001。
每个异步页面应有统一状态：
- idle
- loading
- ready
- empty
- error
- stale
八、代码拆分建议
当前 panel/app.js 建议拆分为：
panel/
  app.js
  navigation/
    router.js
    focus-manager.js
    keymap.js
  ui/
    Layout.js
    Header.js
    Sidebar.js
    Footer.js
    Toast.js
    ConfirmDialog.js
    HelpOverlay.js
    List.js
    Table.js
    ProgressBar.js
  screens/
    DashboardScreen.js
    ServicesScreen.js
    MonitorScreen.js
    ModulesScreen.js
    ChatScreen.js
    DataScreen.js
    SettingsScreen.js
    SetupScreen.js
  api/
    client.js
    monitor.js
    modules.js
    chat.js
    database.js
  config/
    properties.js
    validation.js
拆分顺序应是：
1. 先抽离 API 和配置处理。
2. 再抽离导航和焦点管理。
3. 再抽离通用 UI 组件。
4. 最后将各页面从 app.js 中迁移出去。
不要一开始就重写所有页面，先消除输入冲突和数据安全风险。
九、分阶段实施顺序
P0：安全和核心交互
- 修复 Setup 锁定逻辑。
- 统一 DB 地址配置。
- 移除子页面重复 useInput。
- 修复侧栏焦点误触发。
- 修复 server.properties 增量保存。
- 修复确认框和帮助框的模态行为。
- 修复退出时的进程清理。
P1：统一导航和布局
- 将服务 Tab 合并为服务页。
- 引入统一焦点模型。
- 增加窄屏模式。
- 抽离 Header、Sidebar、Footer。
- 统一滚动、分页和列表焦点。
P2：页面体验
- 重做总览页。
- 重做服务页。
- 重做监控表格。
- 重做模块筛选和依赖展示。
- 重做聊天频道和消息滚动。
- 重做数据库表格浏览。
P3：视觉和反馈
- 重构主题颜色。
- 统一边框、状态符号和标题。
- 增加加载、空状态、错误、过期状态。
- 修复鼠标复制和滚动行为。
- 减少 emoji 对布局的影响。
P4：验证和回归
- 使用 node:test 为纯函数增加测试。
- 测试配置文件往返保存。
- 测试 DB 自定义端口。
- 测试 Setup 未完成时的导航锁定。
- 测试各终端尺寸。
- 测试每个危险操作的确认和取消。
- 使用真实 TTY 手动验证键盘、鼠标、Resize 和 Ctrl+C。
十、验收标准
优化完成后，至少应满足：
- 80x24 终端不出现负高度、覆盖或横向严重溢出。
- Setup 未完成时不能进入其他页面。
- DB 端口改为非 3001 后所有页面仍可用。
- 任意一次按键最多触发一个页面动作。
- 主区上下键不会执行侧栏操作。
- Chat、DB、玩家列表均可滚动和分页。
- 帮助框、确认框、编辑框都是真正模态。
- 保存 server.properties 不丢失未知字段和注释。
- 服务退出后监控数据不会继续显示为有效状态。
- 鼠标提示与实际行为一致。
- 页面不再依赖 app.js 中的大量条件嵌套。
此次仅做了代码观察和方案分析，没有修改文件。当前工作区本身已有大量未提交改动，后续实施前建议先确认当前 TUI 代码基线。
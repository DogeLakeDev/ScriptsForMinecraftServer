# SDK 类型参考

本目录存放从 `@sfmc-bds/sdk` 源码生成的 TypeDoc Markdown（构建时写入 `sdk/`）。

手写导读见 [接口指南 · SDK](../api/sdk/index.md)。

生成命令：`pnpm run docs -- api` / `npm run docs -- api` 或 `pnpm run docs -- build` / `npm run docs -- build`。

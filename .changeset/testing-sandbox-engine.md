---
"@sfmc-bds/sdk": minor
---

feat(sdk/testing): MockBukkit 风格假引擎 + createSandbox + minecraft-loader

- 可控 `@minecraft/server` / `server-ui`（L0 生成全表面 + 未实现硬失败、tick、表单队列）
- `createSandbox` 默认 boot；conformance 套件
- 导出 `@sfmc-bds/sdk/testing/minecraft-loader`

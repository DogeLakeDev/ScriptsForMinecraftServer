---
description: >-
  写代码时不落注释；只汇总「该写注释的位置」交给用户转给 Gemini 补写。
  在当前仓库中始终生效。
alwaysApply: true
---

# 注释交接：代码不写，清单后补

写或改源码时：**不要新增任何注释**（含文件头、JSDoc/`///`、行内与块注释）。现有注释不要顺手删。

需要说明设计意图时，在本轮回复末尾给出一份 **待写注释清单**，由用户交给 Gemini 落笔。清单用简体中文，只写「写在哪、为什么要写」，不要写成可粘贴的成稿注释。

## 该列入清单的位置

- 非显而易见的不变量、兼容边界、版本分派、权限/契约入口
- 容易误读的命名、巧妙/权宜的实现、与文档不一致的现行行为
- 公开 API / 导出符号（将来补 JSDoc 的位置）

不要列入：复述代码的句子、与标识符同义的说明。

## 清单格式

```markdown
## 待写注释

### `相对路径`
- `符号或锚点`：一句说明此处要交代的设计意图（非注释成稿）
```

示例：

```markdown
## 待写注释

### `modules/sdk/@sfmc-sdk/src/module-loader/manifest.ts`
- `validateManifest`：按 schemaVersion 分派 v2/v3；未知版本走同一套 const_mismatch
- `collectCoreIssues`：v2/v3 共用顶层字段，避免两套文案
```

无此类位置则写 `## 待写注释` + `（无）`。本轮只有文档/规则/配置、零源码改动时，可省略清单。

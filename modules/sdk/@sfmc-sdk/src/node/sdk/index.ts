// @sfmc-bds/sdk/node/sdk — Node 模块契约。当前阶段占位。
// 后续 commit 引入 defineNodeModule / NodeHostApis / createNodeModuleController。
/** `@sfmc-bds/sdk/node/sdk` 子路径 semver 版本号。 */
export const SFMC_NODE_SDK_VERSION = "0.1.0" as const;

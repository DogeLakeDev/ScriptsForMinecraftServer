// @sfmc/sdk/node/node — Node-only 工具(HTTP server / sqlite / cron / CLI)。当前阶段占位。
// 真实实现将在 db-server 与 sfmc/ 的迁移 commit 中按需从 db-server/src/* 与 sfmc/src/* 抽取。
export const SFMC_NODE_VERSION = "0.1.0" as const;

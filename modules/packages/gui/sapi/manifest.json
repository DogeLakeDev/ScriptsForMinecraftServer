{
  "schemaVersion": 2,
  "id": "core-gui",
  "name": "主菜单GUI",
  "type": "core",
  "configKey": "gui",
  "requires": ["feature-economy", "feature-chat", "feature-land"],

  "permissions": [
    "service:economy.account.transfer"
  ],

  "services": {
    "provides": [],
    "requires": [
      { "name": "economy.account.transfer", "input": { "type": "object" }, "output": { "type": "object" } }
    ]
  },

  "notes": "core-gui:主菜单/管理面板入口。土地/聊天 GUI 通过 npm 依赖 @sfmc/module-land-gui 与 @sfmc/module-feature-chat 调用;转账走 service.get(economy.account.transfer)。合作社图形 UI 已并入 feature-coop 命令面。"
}

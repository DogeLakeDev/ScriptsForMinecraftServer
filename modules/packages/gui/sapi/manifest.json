{
  "schemaVersion": 2,
  "id": "core-gui",
  "name": "主菜单GUI",
  "type": "core",
  "configKey": "gui",
  "requires": ["feature-economy", "feature-chat", "feature-land", "feature-land-gui"],

  "permissions": [
    "service:economy.account.transfer"
  ],

  "services": {
    "provides": [],
    "requires": [
      { "name": "economy.account.transfer", "input": { "type": "object" }, "output": { "type": "object" } }
    ]
  },

  "notes": "core-gui:主菜单/管理面板入口。依赖 feature-land-gui(LandGUI)与 feature-chat(ChatGUI);转账走 service.get(economy.account.transfer)。合作社图形 UI 已并入 feature-coop 命令面。"
}

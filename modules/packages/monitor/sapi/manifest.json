{
  "schemaVersion": 2,
  "id": "feature-monitor",
  "name": "运行时监控",
  "type": "feature",
  "configKey": "monitor",
  "requires": ["feature-tps"],

  "permissions": [
    "db:read:monitor_metrics",
    "db:write:monitor_metrics",
    "db:read:monitor_player_chunks",
    "db:write:monitor_player_chunks",
    "service:tps.current"
  ],

  "services": {
    "provides": [],
    "requires": [
      { "name": "tps.current" }
    ]
  },

  "notes": "v2 运行时监控:每 600 tick 上报 TPS(从 service tps.current 获取)+ 各维度实体数 + 玩家区块估算。所有数据写自己的表 monitor_metrics / monitor_player_chunks。迁移自 v1 MonitorReporter.ts,@sfmc/module-tps 静态类 import 改为 service.get;HttpDB.post 改为 db.tx + tx.insert。"
}
{
  "schemaVersion": 2,
  "id": "feature-scoreboard-sync",
  "name": "计分板同步",
  "type": "feature",
  "configKey": "scoreboard_sync",
  "requires": [],

  "permissions": [
    "db:read:sfmc_scoreboards",
    "db:write:sfmc_scoreboards"
  ],

  "services": {
    "provides": [],
    "requires": []
  },

  "notes": "v2 计分板数据收集/备份/恢复,写 sfmc_scoreboards(平台 bootstrap 表)。迁移自 v1 HttpDB.post/get 改为 db.tx + tx.insert + db.query。lifecycle.init 时立即备份一次世界计分板;管理员 /scoreboard restore 命令触发从 db 恢复。"
}